services:
  - type: web
    name: ai-news-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn collector:app --bind 0.0.0.0:$PORT --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
